[build]
dockerfile = "Dockerfile"

[deploy]
healthcheckPath = "/Account/Login"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# ?? REQUIRED: Persistent Volume setup (Railway Dashboard) ?????????????????
#
# WITHOUT this, ALL production data is wiped on every Git push / redeploy
# because Railway creates a brand new container each time.
#
# Steps to configure ONCE in the Railway dashboard:
#   1. Open your service ? "Volumes" tab
#      ? Click "Add Volume" ? Mount Path: /data
#   2. Open your service ? "Variables" tab
#      ? Add variable:  DATABASE_PATH = /data/election.db
#
# The app will refuse to start in Production if DATABASE_PATH is not set
# to a /data (or similar volume) path — this prevents silent data loss.
# ??????????????????????????????????????????????????????????????????????????


